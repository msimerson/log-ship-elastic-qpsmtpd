{
  "name": "log-ship-elastic-qpsmtpd",
  "description": "Ship Qpsmtpd logs to Elasticsearch",
  "version": "0.8.1",
  "private": false,
  "files": [
    "lib", "log-ship-elastic-qpsmtpd.ini", "server.js"
  ],
  "keywords": [
    "log",
    "qpsmtpd",
    "elasticsearch",
    "log ship"
  ],
  "homepage": "https://github.com/msimerson/log-ship-elastic-qpsmtpd",
  "bugs": {
    "url": "https://github.com/msimerson/log-ship-elastic-qpsmtpd/issues"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/msimerson/log-ship-elastic-qpsmtpd.git"
  },
  "dependencies": {
    "elasticsearch": "^16.7.3",
    "ini": "^6.0.0",
    "moment-timezone": "^0.6.2",
    "safe-log-reader": "^1.0.8"
  },
  "devDependencies": {
  },
  "license": "BSD-2-Clause",
  "scripts": {
    "lint": "npx eslint .",
    "start": "node server.js",
    "test": "NODE_ENV=test node --test",
    "test:coverage": "NODE_ENV=test node --test --experimental-test-coverage",
    "test:coverage:lcov": "mkdir -p coverage && node --test --experimental-test-coverage --test-reporter=lcov --test-reporter-destination=coverage/lcov.info"
  }
}
